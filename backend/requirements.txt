# FastAPI and web framework
# fastapi 0.115.x pins starlette >=0.40, which fixes CVE-2024-47874
# (unauthenticated multipart-form DoS); 0.109.1 was pinned to vulnerable
# starlette <0.36 and could not be patched without this bump.
fastapi==0.115.6
uvicorn[standard]==0.27.0
gunicorn==23.0.0  # Production WSGI server; 23.0.0 fixes CVE-2024-6827 request smuggling
python-multipart==0.0.22
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Redis for caching
redis==5.0.1
aioredis==2.0.1

# HTTP client for integrations
httpx==0.26.0
aiohttp==3.10.11  # fixes CVE-2024-30251 / CVE-2024-52304 (and related) in 3.9.x

# Image processing
Pillow==10.4.0  # fixes CVE-2024-28219 buffer overflow in the image codecs

# AWS/Azure SDK (for cloud services)
boto3==1.34.25

# Security
cryptography==43.0.1
PyJWT==2.8.0
pyotp==2.9.0  # TOTP MFA
defusedxml==0.7.1  # XML parsing with XXE protection

# Logging and monitoring
structlog==24.1.0
python-json-logger==2.0.7
prometheus-client==0.19.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Development
black==24.1.0
isort==5.13.2
mypy==1.8.0

# PDF generation for audit packets
reportlab==4.0.8
PyPDF2==3.0.1

# Date/time handling
python-dateutil==2.8.2

# Rate limiting
slowapi==0.1.9

# SFTP support for file transfers
paramiko==3.4.0

# Background task scheduling
APScheduler==3.10.4
