# Pilot Environment Configuration
#
# Copy this file to .env.pilot and fill in all required values.
# NEVER commit .env.pilot with real secrets to version control.
#
# Generate secrets with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

# =============================================================================
# REQUIRED - Deployment will fail without these
# =============================================================================

# Database credentials - use strong passwords
POSTGRES_USER=check_review_user
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD
POSTGRES_DB=check_review

# Application secrets - MUST be randomly generated
# Generate each with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE
CSRF_SECRET_KEY=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE
NETWORK_PEPPER=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE
IMAGE_SIGNING_KEY=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE

# =============================================================================
# OPTIONAL - Defaults provided
# =============================================================================

# Image tag for deployments
IMAGE_TAG=latest

# Demo mode (set to true only for demonstrations)
DEMO_MODE=false

# CORS origins - update for your domain
CORS_ORIGINS=["https://your-pilot-domain.bank.com"]

# Frontend API URL (relative path when using nginx reverse proxy)
VITE_API_URL=/api/v1

# Logging level
LOG_LEVEL=INFO
