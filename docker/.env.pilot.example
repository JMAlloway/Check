# =============================================================================
# PILOT ENVIRONMENT CONFIGURATION
# =============================================================================
#
# Copy this file to .env.pilot and fill in all required values.
# NEVER commit .env.pilot with real secrets to version control.
#
# Generate secrets with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# See docs/PILOT_RUNBOOK.md for complete deployment instructions.
#

# =============================================================================
# REQUIRED - Deployment will fail without these
# =============================================================================

# Database credentials
# Use strong, randomly generated passwords (min 24 characters recommended)
POSTGRES_USER=check_pilot_user
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD
POSTGRES_DB=check_review

# Application secrets - MUST be randomly generated for each deployment
# Generate each with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE
CSRF_SECRET_KEY=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE
NETWORK_PEPPER=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE

# =============================================================================
# OPTIONAL - Defaults provided, customize as needed
# =============================================================================

# Docker image tag (use specific version for production stability)
IMAGE_TAG=latest

# Network ports (change if conflicts exist)
HTTP_PORT=80
HTTPS_PORT=443

# Demo mode - set to true only for demonstrations
# WARNING: Demo mode creates synthetic data - never use with real data
DEMO_MODE=false
DEMO_DATA_COUNT=50

# CORS origins - update for your domain(s)
# Must be valid JSON array of allowed origins
CORS_ORIGINS=["https://pilot.yourbank.com"]

# Frontend API URL (relative path when using nginx reverse proxy)
VITE_API_URL=/api/v1

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Image signed URL TTL in seconds
# Shorter = more secure, longer = better UX for slow connections
IMAGE_SIGNED_URL_TTL_SECONDS=90

# =============================================================================
# ADVANCED - Usually don't need to change
# =============================================================================

# Access token expiration (minutes)
# ACCESS_TOKEN_EXPIRE_MINUTES=15

# Session timeout (minutes)
# SESSION_TIMEOUT_MINUTES=30

# Rate limiting
# RATE_LIMIT_PER_MINUTE=100
